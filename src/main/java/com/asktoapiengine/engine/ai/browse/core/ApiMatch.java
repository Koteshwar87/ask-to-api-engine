package com.asktoapiengine.engine.ai.browse.core;

/**
 * (Optional) ApiMatch (only if later needed for structure internally)
 * Since external output is plain text, ApiMatch is optional but still useful internally.
 */
public class ApiMatch {}
